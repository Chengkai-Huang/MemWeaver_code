# MemWeaver

Using conda:
```bash
conda create -n myenv python=3.10
conda activate myenv

pip install -r requirements.txt
```

Run the experiments:
```bash
python test_advanced_kg.py  --model qwen2.5:1.5b --backend ollama
```
